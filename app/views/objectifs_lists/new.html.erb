<div class="page-orga mt-5 mt-sm-0 pt-5 pt-sm-0">

  <header class="me-3 me-sm-5 ms-3 ms-sm-5 p-5">
    <h1>Définir les objectifs</h1>
  </header>

  <% if current_user.is_super_admin? %>
    <div class="d-flex align-items-center justify-content-start me-3 me-sm-5 ms-3 ms-sm-5 pe-5 ps-5 pb-5">
      <%= link_to "Back to objectifs_lists", objectifs_lists_path, class: "btn rounded light pt-1 pb-1 pe-3 ps-3 btn-primary" %>
    </div>
  <% end %>

  <%= render "form", objectifs_list: @objectifs_list %>

  <br>

</div>
